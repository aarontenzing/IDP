theory T:V {
//ColourOf(Belgium) = Red.
//ColourOf(Luxembourg) ~= ColourOf(Belgium).
//!x in country: ColourOf(x) = Yellow.
//!x in country: Bordering(x,Germany) => ColourOf(x) = Green.
//!x in country: ~Bordering(x,Switzerland) => ColourOf(x) = Yellow. 
!x,y  in country: Bordering(x,y) => ColourOf(x) ~= ColourOf(y).
}